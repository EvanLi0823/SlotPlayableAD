# 横竖屏适配系统修复说明

## 修复日期
2026-01-22

## 问题描述
项目导出的playable无法满足"支持横竖屏自由旋转"的要求。主要问题包括：
- 方向检测使用了错误的API
- 缺少原生设备旋转事件监听
- 检测响应速度慢
- Canvas适配模式可能不兼容某些平台

## 修改内容

### 1. 修复方向检测API ✅
**文件**：`assets/Script/adaptation/OrientationAdaptationManager.ts`

**修改内容**：
- **行270-291**：`detectOrientation()` 方法
  - 改用 `cc.view.getVisibleSize()` 替代 `cc.view.getFrameSize()`
  - 添加对 `window.orientation` 的优先检测（移动设备）
  - 增加详细的日志输出

- **行348-351**：`updateScreenInfo()` 方法
  - 改用 `visibleSize` 作为主要计算依据

### 2. 添加原生事件监听 ✅
**文件**：`assets/Script/adaptation/OrientationAdaptationManager.ts`

**修改内容**：
- **行70**：添加 `resizeDebounceTimer` 属性
- **行471-532**：增强 `registerSystemEvents()` 方法
  - 添加 `orientationchange` 事件监听
  - 添加 `resize` 事件监听
  - 添加 `fullscreenchange` 事件监听
  - 实现防抖处理机制

### 3. 优化检测频率 ✅
**文件**：`assets/Script/adaptation/OrientationAdaptationManager.ts`

**修改内容**：
- **行452-455**：将检测间隔从500ms降低到200ms

### 4. 改进Canvas适配策略 ✅
**文件**：
- `assets/Script/adaptation/strategies/LandscapeStrategy.ts`
- `assets/Script/adaptation/strategies/PortraitStrategy.ts`

**修改内容**：
- **两文件的setupCanvas方法**（行75-95）：
  - 添加平台检测（`cc.sys.isMobile`）
  - 移动设备使用 SHOW_ALL 模式（fitWidth=true, fitHeight=true）
  - 桌面设备使用原配置
  - 添加详细日志输出

### 5. 初始化和清理优化 ✅
**文件**：
- `assets/Script/adaptation/OrientationAdaptationManager.ts`
- `assets/Script/GameScene.ts`

**修改内容**：

**OrientationAdaptationManager.ts**：
- **行116-123**：初始化时立即应用正确策略
- **行208-232**：添加 `forceUpdate()` 方法
- **行253-279**：增强 `destroy()` 方法，完整清理资源

**GameScene.ts**：
- **行204-213**：添加延迟初始化，确保节点加载完成后强制应用策略

## 核心改进

### 检测机制改进
1. **双重检测**：优先使用 `window.orientation`，备用宽高比检测
2. **正确的尺寸API**：使用 `getVisibleSize()` 获取实际可视区域
3. **多事件源**：同时监听多个相关事件，提高响应可靠性

### 响应速度提升
1. **检测频率**：500ms → 200ms
2. **事件驱动**：添加原生事件立即响应
3. **防抖处理**：避免频繁触发

### 平台兼容性
1. **移动优先**：移动设备使用 SHOW_ALL 适配模式
2. **平台检测**：根据不同平台使用不同策略
3. **降级方案**：多种检测方法确保兼容性

## 测试建议

### 测试平台
- iOS Safari（iPhone/iPad）
- Android Chrome
- 微信小游戏
- Facebook Instant Games
- Unity WebGL

### 测试场景
1. **初始方向检测**
   - 横屏启动
   - 竖屏启动
   - 45度角启动

2. **旋转响应**
   - 快速旋转
   - 慢速旋转
   - 连续多次旋转

3. **UI表现**
   - 所有UI元素位置正确
   - 无变形或裁剪
   - 动画过渡流畅

4. **性能测试**
   - CPU占用
   - 内存泄漏
   - 帧率稳定性

### 调试模式
在GameScene的Inspector中启用：
- `Enable Adaptation` = true
- `Enable Safe Area` = true
- `Adaptation Debug Mode` = true（查看详细日志）

### 浏览器控制台命令
```javascript
// 查看当前方向
adaptationManager.getCurrentOrientation()

// 强制更新
adaptationManager.forceUpdate()

// 查看屏幕信息
adaptationManager.getScreenInfo()
```

## 已知限制
1. 某些嵌入式WebView可能不支持 `orientationchange` 事件
2. 桌面浏览器窗口拖拽调整大小时使用宽高比检测
3. 首次加载可能有短暂延迟（已通过forceUpdate缓解）

## 建议后续优化
1. 添加配置选项控制检测频率
2. 实现渐进式加载策略
3. 添加性能监控和统计
4. 考虑使用 ResizeObserver API（需要兼容性处理）

## 回滚方案
如需回滚，可通过git恢复以下文件：
- `assets/Script/adaptation/OrientationAdaptationManager.ts`
- `assets/Script/adaptation/strategies/LandscapeStrategy.ts`
- `assets/Script/adaptation/strategies/PortraitStrategy.ts`
- `assets/Script/GameScene.ts`

## 联系方式
如有问题，请联系开发团队。